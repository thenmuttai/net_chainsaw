cmake_minimum_required(VERSION 3.31)

# Project name
project(NetChainsaw)

# Include directories
include_directories(include)

# Add helper library
add_library(helper STATIC src/helper.c)

# Add subdirectory for -> (1) TimeServer
add_subdirectory(tryouts/time_server)

